---
phase: 08-m-api-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify_mplus_api.py
autonomous: false
requirements:
  - BENCH-01
must_haves:
  truths:
    - "Verification script queries WCL live with difficulty=10 and returns M+ ranking data"
    - "Verification script tests bracket parameter for keystone level filtering"
    - "Verification script confirms dungeon encounter IDs from get_encounters(content_type='mythic_plus')"
    - "Verification script checks keystoneLevel/keystoneBonus fields in report fights"
  artifacts:
    - path: "scripts/verify_mplus_api.py"
      provides: "Live WCL M+ API parameter verification"
      min_lines: 80
  key_links:
    - from: "scripts/verify_mplus_api.py"
      to: "src/wcl_client.WCLClient"
      via: "direct import and query"
      pattern: "from src.wcl_client import WCLClient"
    - from: "scripts/verify_mplus_api.py"
      to: "src/tools/encounters.py"
      via: "get_encounters call"
      pattern: "get_encounters.*mythic_plus"
---

<objective>
Create and run a verification script that tests WCL M+ API parameters live before building any production code (per D-01). This script validates: difficulty=10 works for M+ rankings, bracket parameter accepts raw keystone integer, dungeon encounter IDs are discoverable, and keystoneLevel/keystoneBonus fields exist in report fight data.

Purpose: Avoid building production code on unverified API assumptions. Medium-confidence parameters (difficulty=10, bracket format) must be confirmed live.
Output: scripts/verify_mplus_api.py + human-verified API response documentation
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-m-api-foundation/08-CONTEXT.md
@.planning/phases/08-m-api-foundation/08-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/wcl_client.py:
```python
class WCLClient:
    def __init__(self, client_id: str, client_secret: str): ...
    async def query(self, graphql: str, *, with_rate_limit: bool = True) -> dict: ...
    async def close(self) -> None: ...
```

From src/tools/encounters.py:
```python
async def get_encounters(
    client: WCLClient,
    content_type: Optional[Literal["raid", "mythic_plus", "all"]] = "all",
) -> EncountersResponse:
```

From src/models.py:
```python
class EncountersResponse(BaseModel):
    expansion: str = ""
    zones: list[Zone] = []

class Zone(BaseModel):
    id: int
    name: str
    encounters: list[Encounter] = []

class Encounter(BaseModel):
    id: int
    name: str
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create M+ API verification script</name>
  <files>scripts/verify_mplus_api.py</files>
  <read_first>
    - src/wcl_client.py
    - src/tools/encounters.py
    - src/tools/timelines.py (lines 135-172: _query_rankings pattern)
    - src/tools/dungeon_analysis.py (lines 80-100: _query_all_fights GraphQL)
    - .planning/phases/08-m-api-foundation/08-RESEARCH.md (verification script example)
  </read_first>
  <action>
Create `scripts/verify_mplus_api.py` following the structure in RESEARCH.md code example. The script MUST:

1. **Step 1 - Discover M+ dungeon encounter IDs:**
   - Call `get_encounters(client, content_type="mythic_plus")`
   - Print all zones and encounter IDs
   - If zero zones found, print ERROR and exit

2. **Step 2 - Test difficulty=10 (no bracket):**
   - Use the first discovered encounter ID
   - Query `characterRankings` with `difficulty: 10`, `className: "Mage"`, `specName: "Frost"`, `metric: dps`
   - Print encounter name, rankings count, hasMorePages, first ranking details
   - Print `bracketData` value (should be keystone level, not ilvl)
   - If rankings count is 0, print WARNING and try without difficulty parameter as fallback

3. **Step 3 - Test bracket parameter:**
   - Same query as Step 2 but add `bracket: 12`
   - Print rankings count and first ranking's bracketData
   - Compare with Step 2 to confirm filtering works

4. **Step 4 - Test keystoneLevel/keystoneBonus fields:**
   - From Step 2 rankings, take first ranking's report code
   - Query `reportData.report.fights` with fields: `id, encounterID, name, keystoneLevel, keystoneBonus, keystoneAffixes, keystoneTime, gameZone { id name }`
   - Print first 5 fights showing which have keystoneLevel populated

Script must:
- Use `WCL_CLIENT_ID` and `WCL_CLIENT_SECRET` env vars
- Print clear section headers with `===` separators
- Print full JSON of first ranking entry for documentation
- Handle errors gracefully with informative messages
- Use Chinese comments following project convention (per D-01, Claude's discretion on script structure)

Per D-01: This is script-first verification. Build code only after verification passes.
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('scripts/verify_mplus_api.py').read()); print('syntax ok')"</automated>
  </verify>
  <acceptance_criteria>
    - File `scripts/verify_mplus_api.py` exists and is valid Python
    - Contains `from src.wcl_client import WCLClient`
    - Contains `get_encounters(client, content_type="mythic_plus")` call
    - Contains `difficulty: 10` in a GraphQL query string
    - Contains `bracket: 12` in a GraphQL query string
    - Contains `keystoneLevel` in a GraphQL query string
    - Contains `keystoneBonus` in a GraphQL query string
    - Contains `WCL_CLIENT_ID` and `WCL_CLIENT_SECRET` env var reads
    - Has 4 verification steps clearly labeled
  </acceptance_criteria>
  <done>Verification script exists, parses cleanly, and contains all 4 verification steps targeting difficulty=10, bracket filtering, encounter discovery, and keystone fields</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run verification script and confirm API responses</name>
  <files>scripts/verify_mplus_api.py</files>
  <what-built>M+ API verification script that tests WCL parameters live</what-built>
  <action>
This is a human-verify checkpoint. The executor presents the verification script to the user and waits for them to run it with live WCL credentials. No automated action — the script was built in Task 1.
  </action>
  <how-to-verify>
    1. Ensure WCL_CLIENT_ID and WCL_CLIENT_SECRET are set in environment
    2. Run: `python scripts/verify_mplus_api.py`
    3. Confirm these results:
       - Step 1: At least 1 M+ dungeon zone with encounter IDs printed
       - Step 2: Rankings count > 0 for difficulty=10, bracketData shows keystone level (integer 8-15), NOT item level (600+)
       - Step 3: bracket=12 returns rankings with bracketData == 12 (or close)
       - Step 4: At least one fight has keystoneLevel > 0
    4. If any step fails, describe what happened — this blocks Plan 02
  </how-to-verify>
  <verify>
    <automated>test -f scripts/verify_mplus_api.py && echo "script exists"</automated>
  </verify>
  <acceptance_criteria>
    - User has run the script and reported results
    - difficulty=10 behavior is documented (works or alternative identified)
    - bracket parameter behavior is documented
    - keystoneLevel field availability is confirmed
  </acceptance_criteria>
  <done>Human has verified WCL M+ API parameters work correctly (or identified necessary adjustments) and documented the results</done>
  <resume-signal>
    Type "verified" with a brief summary of results (encounter IDs found, bracket behavior, keystone fields), OR describe issues found.
    If bracket parameter does not work as expected, describe what bracketData values look like.
  </resume-signal>
</task>

</tasks>

<verification>
- scripts/verify_mplus_api.py exists and is syntactically valid
- Script contains all 4 verification steps
- Human has run the script and confirmed API parameter behavior
</verification>

<success_criteria>
- Verification script created and tested against live WCL API
- difficulty=10 confirmed to return M+ ranking data (or alternative identified)
- bracket parameter behavior documented (works as raw integer or alternative encoding identified)
- keystoneLevel/keystoneBonus field availability confirmed
- Results documented so Plan 02 can proceed with correct parameters
</success_criteria>

<output>
After completion, create `.planning/phases/08-m-api-foundation/08-01-SUMMARY.md`
</output>
