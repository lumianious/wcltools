# WarcraftLogs API — https://www.warcraftlogs.com/api/clients
WCL_CLIENT_ID=
WCL_CLIENT_SECRET=

# The executable reads environment variables, not this file automatically.
# Prefer `wcltools auth configure` to store WCL credentials in the OS keyring.
# No Blizzard credentials are needed to use WCLTools.
